import { SetBoxshadowOnHoverDirective } from './set-boxshadow-on-hover.directive';

describe('SetBoxshadowOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new SetBoxshadowOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
