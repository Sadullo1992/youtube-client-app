import { SelectBoxShadowColorDirective } from './select-box-shadow-color.directive';

describe('SelectBoxShadowColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectBoxShadowColorDirective();
    expect(directive).toBeTruthy();
  });
});
