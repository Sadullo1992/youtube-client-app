import { SelectBorderColorDirective } from './select-border-color.directive';

describe('SelectBorderColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectBorderColorDirective();
    expect(directive).toBeTruthy();
  });
});
